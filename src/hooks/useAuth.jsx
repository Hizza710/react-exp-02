import { useEffect } from "react";

/**
 * Minimal placeholder for useAuth hook.
 *
 * This file exists so imports like `import useAuth from "../../hooks/useAuth"`
 * resolve correctly during Vite import analysis.
 *
 * Replace this implementation with real authentication logic (token check,
 * redirects, context usage, etc.) as needed.
 */
export default function useAuth() {
    useEffect(() => {
        // placeholder: add auth checks here
    }, []);
}
